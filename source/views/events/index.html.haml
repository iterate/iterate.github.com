---
title: Arrangementer
layout: application
---

#main

  -# MANUAL PART -- remove or add sections as you please.
  -# Copy a section below, add a suitable background image in _events.scss
  -# h2 gives a smaller headline while h3 is shown as the main title of the section.

  .startupmixer.has-background-image
    %section
      .two-thirds-layout-offset
        %h2 Oslo Innovation Week Special
        %h3 Innovation Cocktail
        %h4 How big corporations can innovate like startups, and how startups can grow to big businesses?
        %p Oslo, 19. 10. 2016
        %p A designer, a programmer and a business developer will share their thoughts and learnings.
        %p.has-padding-top
          =link_to 'Come, join!', 'https://innovationcocktail.splashthat.com/', :class => 'button'

  -# AUTOMAGIC PART -- only touch if you know what you're doing =)
  -# Magic is based on meta data provided in the front matter headers of the single event pages,
  -# and uses Middleman's sitemap functionality

  - current_resource.children.each do | page |
    %div{ :class => [('white' unless page.data.featured?), ('light-colored has-background-image' if page.data.featured && page.data.serie == 'lean-cortado')], :style => ('background-image: url(' + image_path('headers/' + page.data.banner) + ');' if page.data.featured) }
      %section{ :class => ('featured-event' if page.data.featured) }
        %h4= link_to page.data.title + ' – ' + page.data.subtitle, page.url

        %p= page.data.date + ' – ' + page.data.time
        %p= page.data.short_description
        - if page.data.presenter
          %p
            Presentert av
            = data.people[page.data.presenter].name
